use std::fmt;

#[derive(Debug, Clone)]
pub enum Function {

}

pub fn parse_function(s: &str) -> Option<Keyword> {
}

impl fmt::Display for Keyword {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        match self {
        }
    }
}